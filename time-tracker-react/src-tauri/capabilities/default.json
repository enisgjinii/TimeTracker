{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window with system monitoring",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
